import { pageMetadata } from "@/i18n/metadata";
import PageClient from "./page-client";
export default PageClient;

export function generateMetadata() { return pageMetadata("help_owned_operators"); }
