import zh from "../../../messages/helpers/zh/rotation_settings.json" with { type: "json" };
import en from "../../../messages/helpers/en/rotation_settings.json" with { type: "json" };
import { createLocalizer } from "../localize.ts";
export const localize = createLocalizer(zh, en);
