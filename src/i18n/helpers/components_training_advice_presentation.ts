import zh from "../../../messages/helpers/zh/components_training_advice_presentation.json" with { type: "json" };
import en from "../../../messages/helpers/en/components_training_advice_presentation.json" with { type: "json" };
import { createLocalizer } from "../localize.ts";
export const localize = createLocalizer(zh, en);
