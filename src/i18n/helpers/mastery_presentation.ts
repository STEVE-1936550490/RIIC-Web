import zh from "../../../messages/helpers/zh/mastery_presentation.json" with { type: "json" };
import en from "../../../messages/helpers/en/mastery_presentation.json" with { type: "json" };
import { createLocalizer } from "../localize.ts";
export const localize = createLocalizer(zh, en);
